version: "2"
linters:
  default: none
  enable:
    - revive
    - gocritic
    - errcheck
    - staticcheck
    - gosec
    - unparam
    - wastedassign
    - nilnil
    - errorlint
    - tagliatelle
    - forbidigo
    - depguard
  settings:
    tagliatelle:
      case:
        rules:
          json: camel
    forbidigo:
      forbid:
        - pattern: '^slog\.Default\(\)'
          msg: use injected logger (kernel/log.New), not slog.Default
    revive:
      rules:
        - name: exported
          disabled: true
    gosec:
      excludes:
        - G104
    depguard:
      rules:
        kernel_no_plugins:
          files:
            - "**/internal/kernel/**"
          deny:
            - pkg: "github.com/ageneralai/maven/internal/plugins/"
              desc: "kernel must not import plugins"
  exclusions:
    rules:
      - path: cmd/
        linters:
          - forbidigo
          - gosec
          - gocritic
      - path: _test\.go
        linters:
          - gosec
          - unparam
      - path: internal/plugins/channel/
        linters:
          - tagliatelle
          - gosec
      - path: internal/plugins/voice/
        linters:
          - tagliatelle
          - gosec
      - path: internal/modality/
        linters:
          - tagliatelle
          - gosec
      - path: internal/plugins/tool/acp/
        linters:
          - gosec
      - path: internal/plugins/memory/
        linters:
          - gosec
      - path: internal/kernel/config/
        linters:
          - tagliatelle
      - path: internal/kernel/memory/
        linters:
          - gosec
      - path: internal/kernel/prompt/
        linters:
          - gosec
      - path: internal/kernel/channel/webhook/
        linters:
          - gosec
      - path: internal/testutil/
        linters:
          - gosec
      - path: internal/kernel/slash/
        linters:
          - gocritic
      - path: internal/plugins/channel/whatsapp/
        linters:
          - gocritic
      - path: internal/plugins/channel/telegram/stream_state\.go
        linters:
          - unparam
run:
  timeout: 5m
